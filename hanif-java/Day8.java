import java.time.LocalDateTime;
import java.time.LocalTime;
import java.time.ZoneId;
import java.time.ZonedDateTime;
import java.time.temporal.ChronoUnit;

public class Day8 {

    public static void main(String[] args) {
        // long waktu = System.currentTimeMillis();
        // LocalTime localTime = LocalTime.now();
        // System.out.println("Current local time: " + localTime);

        // ZonedDateTime zonedDateTime = ZonedDateTime.now(ZoneId.of("UTC+4"));
        // System.out.println("Current time in UTC+4: " + zonedDateTime);

        // LocalDateTime startDate = LocalDateTime.parse("2022-03-10T10:55:45.920");
        // LocalDateTime endDate = LocalDateTime.parse("2024-06-19T08:40:35.300");

        // System.out.println("Start date: " + startDate);
        // System.out.println("End date: " + endDate);

        
        // long totalMilliseconds = ChronoUnit.MILLIS.between(startDate, endDate);

        // System.out.println(totalMilliseconds );

        
        // long millisInSecond = 1000;
        // long millisInMinute = millisInSecond * 60;
        // long millisInHour = millisInMinute * 60;
        // long millisInDay = millisInHour * 24;
        // long millisInMonth = millisInDay * 30; 
        // long millisInYear = millisInDay * 365; 

        
        // long years = totalMilliseconds / millisInYear;
        // totalMilliseconds %= millisInYear;

        // long months = totalMilliseconds / millisInMonth;
        // totalMilliseconds %= millisInMonth;

        // long days = totalMilliseconds / millisInDay;
        // totalMilliseconds %= millisInDay;

        // long hours = totalMilliseconds / millisInHour;
        // totalMilliseconds %= millisInHour;

        // long minutes = totalMilliseconds / millisInMinute;
        // totalMilliseconds %= millisInMinute;                                                                                                                                                                                                                                                                                                                                                                                             

        // long seconds = totalMilliseconds / millisInSecond;
        // totalMilliseconds %= millisInSecond;

        // long milliseconds = totalMilliseconds;

        // System.out.println("Difference: " + years + " years, " + months + " months, " + days + " days, " +
        //         hours + " hours, " + minutes + " minutes, " + seconds + " seconds, " + milliseconds + " milliseconds");
        // System.out.println("Total difference in milliseconds: " + ChronoUnit.MILLIS.between(startDate, endDate));



        
    }
}
